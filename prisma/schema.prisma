generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model User {
  user_id   String   @id @default(auto()) @map("_id") @db.ObjectId
  name      String   @unique
  password  String
  salt      String
  email     String   @unique
  role      UserRole @default(USER)
  // relational
  phews     Phew[]
  folders   Folder[]
  // 
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

enum UserRole {
  USER
  ROOT
  STEM
}

model Phew {
  phew_id          String   @id @default(uuid()) @map("_id") @db.ObjectId
  name             String
  content          String
  password         String
  salt             String
  // relational
  user_id          String   @db.ObjectId
  user             User     @relation(fields: [user_id], references: [user_id])
  parent_folder_id String   @db.ObjectId
  parent           Folder   @relation(fields: [parent_folder_id], references: [folder_id])
  // 
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
}

model Folder {
  folder_id        String   @id @default(uuid()) @map("_id") @db.ObjectId
  name             String
  // relational
  user_id          String   @db.ObjectId
  user             User     @relation(fields: [user_id], references: [user_id])
  phews            Phew[]
  Folders          Folder[] @relation(name: "parent")
  parent           Folder?  @relation(name: "parent", fields: [parent_folder_id], references: [folder_id], onDelete: NoAction, onUpdate: NoAction)
  parent_folder_id String?  @db.ObjectId
  // 
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
}
